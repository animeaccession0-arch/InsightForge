"scripts": {
  "dev": "vite",
  "build": "vite build",
  "start": "vite preview",
  "preview": "vite preview",
  "check": "tsc --noEmit || true",
  "format": "prettier --write .",
  "test": "vitest",
  "db:push": "drizzle-kit push"
}
