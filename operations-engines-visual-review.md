# Operations engines visual verification

The focused desktop Heritage QC workspace shows the Spatial Lux-Transmission Sounding Estimator with a clear directional-light video drop zone and screening scope. The focused Predictive Vending workspace shows the Multi-Agent Swarm Routing Engine with endpoint CSV upload, fleet-size selection, simulation action, 50-endpoint cap explanation, and empty-state guidance. Existing shock simulation, export controls, and dark responsive shell remain visible and readable.

## Mobile verification

The narrow mobile captures show the KuraVision video drop zone stacking cleanly beneath the existing Heritage scanner, while the VeloRoute endpoint CSV textarea, vehicle selector, Simulate swarm button, shock controls, and transfer-engine empty state remain reachable without horizontal overflow. The output panels are intentionally empty until the user supplies real video or endpoint data.
